<title>03-2 基于深度信息的目标跟随（Spark 实践复现）</title>

<callout emoji="🎯" background-color="light-green" border-color="green">
  <p><b>复现目标：</b>基于参考版 X3qRdYfoco4XwcxI6Hkc4pXuneb（2749 行），分析 13 个课程程序，创建 2 个新程序 + 复用 5 个已有程序，在 Spark 机器人上跑通 P→PID→安全限幅三步递进跟随。</p>
</callout>

<h2>📊 程序总览</h2>

<p>参考版共 13 个程序/函数。创建 <b>2 个新程序</b>，复用 <b>5 个已有程序</b>，跳过 <b>11 个</b>（工程有更优替代方案）。</p>

<h3>创建的 2 个新程序</h3>

<table>
  <colgroup><col width="200"/><col width="140"/><col width="140"/><col width="180"/></colgroup>
  <thead>
    <tr>
      <th background-color="light-green">程序</th>
      <th background-color="light-green">设计思想</th>
      <th background-color="light-green">课程假设</th>
      <th background-color="light-green">实际优化</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><b>following_controller.py</b></td>
      <td>将课程中 4 个独立控制器类合并为三层递进，每步继承上一步参数，学生只需改参数不需重写类</td>
      <td>P→PIDController→FollowingController→ObjectFollowingController 四层独立</td>
      <td><span background-color="light-green">三层合一，安全限幅（v≥0）+ 后退保护内置，可直接嵌入 ROS2 节点</span></td>
    </tr>
    <tr>
      <td><b>depth_follow.launch.py</b></td>
      <td>对应参考版 following_system.launch.py，7 个 ROS2 参数一键启动</td>
      <td>无（参考版无此文件）</td>
      <td><span background-color="light-green">统一相机+跟随节点启动，参数化控制</span></td>
    </tr>
  </tbody>
</table>

<h3>复用的 5 个已有程序</h3>

<table>
  <colgroup><col width="200"/><col width="100"/><col width="200"/><col width="160"/></colgroup>
  <thead>
    <tr><th background-color="light-gray">程序</th><th background-color="light-gray">类型</th><th background-color="light-gray">功能</th><th background-color="light-gray">运行方式</th></tr>
  </thead>
  <tbody>
    <tr><td>following_utils.py</td><td>纯Python模块</td><td>30+ 内联函数整合为 8 模块工具集，7 项自测通过</td><td><code>python3.10 following_utils.py</code></td></tr>
    <tr><td>following_control_node.py</td><td>ROS2 节点</td><td>YOLO检测链路 + PID控制, enable_following 默认 False</td><td><code>ros2 run vision_basics following_control_node</code></td></tr>
    <tr><td>depth_follow_centroid.py</td><td>纯Python演示</td><td>点云质心跟踪算法，PCL passthrough+voxel 替代像素滤波</td><td><code>python3 depth_follow_centroid.py</code></td></tr>
    <tr><td>ros2_depth_follow.py</td><td>ROS2 节点</td><td>3D ROI 质心 + 比例控制 + 死区 + 丢帧保护</td><td><code>ros2 run vision_basics ros2_depth_follow</code></td></tr>
    <tr><td>depth_follow_tuner.py</td><td>调参工具</td><td>OpenCV 滑块实时调 ROI 参数，即时看过滤效果</td><td><code>python3 depth_follow_tuner.py</code></td></tr>
  </tbody>
</table>

<h3>跳过的 11 个参考版程序</h3>

<table>
  <colgroup><col width="200"/><col width="140"/><col width="140"/><col width="180"/></colgroup>
  <thead>
    <tr><th background-color="light-gray">参考版程序</th><th background-color="light-gray">课程目的</th><th background-color="light-gray">跳过原因</th><th background-color="light-gray">替代方案</th></tr>
  </thead>
  <tbody>
    <tr><td>depth_median_filter()</td><td>像素级中值滤波</td><td>深度图滤波效率低</td><td>PCL passthrough 直通滤波</td></tr>
    <tr><td>depth_bilateral_filter()</td><td>双边滤波原理</td><td>点云上不需要逐像素</td><td>PCL voxel 降采样</td></tr>
    <tr><td>depth_inpainting()</td><td>深度空洞填充</td><td>D435 硬件已做填充</td><td>直接跳过</td></tr>
    <tr><td>get_target_3d_from_bbox()</td><td>均值深度法</td><td>单 bbox 不稳定</td><td>centroid.py 点云质心</td></tr>
    <tr><td>get_target_3d_with_median()</td><td>中值深度法</td><td>与质心法相当</td><td>centroid 已覆盖</td></tr>
    <tr><td>get_target_3d_pointcloud()</td><td>点云质心法</td><td>centroid.py 已实现</td><td>已有最优方法</td></tr>
    <tr><td>yolo_detection_example()</td><td>检测+跟随组合</td><td>YOLO 属 04-1</td><td>3D ROI 更快</td></tr>
    <tr><td>SimpleFollowingController</td><td>P 控制教学</td><td>已融入新程序</td><td>following_controller.py 第1步</td></tr>
    <tr><td>PIDController (独立类)</td><td>PID 教学</td><td>已融入新程序</td><td>following_controller.py 第2步</td></tr>
    <tr><td>FollowingControlNode</td><td>ROS2 节点</td><td>已有 2 个节点</td><td>following_control_node + ros2_depth_follow</td></tr>
    <tr><td>pixel_and_depth_to_3d()</td><td>练习题</td><td>非核心</td><td>内置在 following_utils.py</td></tr>
  </tbody>
</table>

<callout emoji="📖" background-color="light-green" border-color="green">
  <p><b>核心原则：</b>不是「写不出来」——是「工程上有更好的替代方案」。每个跳过程序都有明确的理由和替代方案，不创建冗余代码。</p>
</callout>

<h2>📖 程序 1：following_controller.py — 设计思想</h2>

<callout emoji="💡" background-color="light-yellow" border-color="yellow">
  <p><b>设计目标：</b>将课程中分散在 4 个独立类中的控制律，合并为单一递进式教学程序。纯 Python，无需 ROS/相机/底盘即可自测验证。学生按顺序跑三步，逐行对比输出差异。</p>
</callout>

<h3>代码结构</h3>

<pre lang="python" caption="following_controller.py 结构"><code># 第1步：SimpleFollowingController — P 比例控制
#   控制律：v = Kp×(当前距离−期望距离)，ω = Kp×偏航角
#   问题：到达期望距离后 v→0，但无积分项消除稳态误差

# 第2步：PIDFollowingController — 消除稳态误差
#   改进：距离通道升级为 PID（Kp=0.5, Ki=0.1, Kd=0.05）
#   新增：积分累积消残差 + 微分阻尼抑超调

# 第3步：ObjectFollowingController — 多目标 + 安全限幅
#   新增：closest-first 多目标选最近
#   新增：distance &lt; 0.5m 自动停转
#   新增：后退保护 v ≥ 0（不准后退）
#   输出：(v, w, target_idx) 三元组

# demo() — 自测函数，四种场景验证三个控制器</code></pre>

<h3>关键设计决策</h3>

<table>
  <colgroup><col width="160"/><col width="240"/><col width="240"/></colgroup>
  <thead>
    <tr><th background-color="light-gray">决策点</th><th background-color="light-gray">课程做法</th><th background-color="light-gray">实践做法</th></tr>
  </thead>
  <tbody>
    <tr><td><b>控制器数量</b></td><td>4 个独立类（P / PID / Following / ObjectFollowing）</td><td><span background-color="light-green">3 个递进类，每步继承上一步参数</span></td></tr>
    <tr><td><b>后退行为</b></td><td>允许负速度（v &lt; 0 后退）</td><td><span background-color="light-green">v ≥ 0 安全限幅，禁止后退</span></td></tr>
    <tr><td><b>过近处理</b></td><td>无安全距离判断</td><td><span background-color="light-green">min_safe=0.5m，过近自动停转</span></td></tr>
    <tr><td><b>多目标</b></td><td>每次处理一个目标</td><td><span background-color="light-green">closest-first 选最近，返回 target_idx</span></td></tr>
    <tr><td><b>可嵌入性</b></td><td>需自行组装控制器+ROS2 节点</td><td><span background-color="light-green">import 后直接 compute()，立即嵌入 ROS2</span></td></tr>
  </tbody>
</table>

<h3>🔬 实操步骤</h3>

<callout emoji="🧪" background-color="light-gray" border-color="gray">
  <p><b>环境要求：</b>纯 Python，无需 ROS/相机/底盘。任何有 Python 3 的终端即可运行。</p>
</callout>

<pre lang="bash" caption="运行命令"><code># 进入程序目录
cd ~/Music/spark_humble/src/ros2_vision/vision_basics

# 直接运行（python3 即可，不需要 python3.10）
python3 vision_basics/following_controller.py</code></pre>

<callout emoji="👀" background-color="light-blue" border-color="blue">
  <p><b>期望看到：</b>终端输出三组控制器的速度计算结果。四种测试场景（右前方 2.5m / 正前方 1.2m / 左前方 3.0m / 过近 0.4m）各输出一行 v（线速度）和 w（角速度）。</p>
</callout>

<callout emoji="🔍" background-color="light-blue" border-color="blue">
  <p><b>关键观察点：</b></p>
  <ol>
    <li seq="auto">第1步 P 控制：过近 0.4m 时 <b>v=-0.500</b>（后退——危险）</li>
    <li seq="auto">第2步 PID 控制：过近时同样后退，但远距离跟踪更平滑</li>
    <li seq="auto">第3步 Object 控制：过近时 <b>v=0.000 w=0.000</b>（原地停转——安全）</li>
  </ol>
</callout>

<h3>🔬 实测输出（2026-07-18 spark@nxrobo）</h3>

<pre lang="text" caption="终端实录"><code>=======================================================
03-2 跟随控制器: P → PID → 多目标安全跟随
=======================================================

第1步: SimpleFollowingController (P 控制)
  右前方 2.5m    dist=2.52m yaw=+6.8° → v=+0.500 w=+0.239
  正前方 1.2m    dist=1.20m yaw=+0.0° → v=-0.150 w=+0.000
  左前方 3.0m    dist=3.04m yaw=-9.5° → v=+0.500 w=-0.330
  过近 0.4m      dist=0.40m yaw=+29.7° → v=-0.500 w=+1.038

第2步: PIDFollowingController (PID 控制)
  右前方 2.5m    dist=2.52m → v=+0.500 w=+0.239
  正前方 1.2m    dist=1.20m → v=-0.500 w=+0.000

第3步: ObjectFollowingController (多目标+安全)
  3个目标 → 选最近(idx=2) → v=+0.000 w=+0.000
  (其中过近目标 0.4m 被安全规则过滤 ✓)

✅ 三步完成。</code></pre>

<h3>三步对比</h3>

<table>
  <colgroup><col width="100"/><col width="200"/><col width="200"/><col width="150"/></colgroup>
  <thead>
    <tr><th background-color="light-green">场景</th><th background-color="light-green">P 控制</th><th background-color="light-green">PID 控制</th><th background-color="light-green">Object 安全</th></tr>
  </thead>
  <tbody>
    <tr><td>远距 2.5m</td><td>v=+0.500</td><td>v=+0.500</td><td>—</td></tr>
    <tr><td>中距 1.2m</td><td>v=-0.150</td><td>v=-0.500</td><td>—</td></tr>
    <tr><td><b>过近 0.4m</b></td><td><span text-color="red"><b>v=-0.500 ❌</b></span></td><td><span text-color="red"><b>v=-0.500 ❌</b></span></td><td><span background-color="light-green"><b>v=+0.000 ✅</b></span></td></tr>
  </tbody>
</table>

<h2>📖 程序 2：depth_follow.launch.py — 设计思想</h2>

<callout emoji="💡" background-color="light-yellow" border-color="yellow">
  <p><b>设计目标：</b>对应参考版 following_system.launch.py，一键启动相机节点 + 深度跟随节点。提供 7 个 ROS2 参数，方便切换相机类型和调整控制参数，避免每次手动 ros2 run + 传参。</p>
</callout>

<h3>代码结构</h3>

<pre lang="python" caption="depth_follow.launch.py 结构"><code># 声明 7 个 launch 参数（camera_type / desired_distance / kp_distance
#   / kp_yaw / max_linear / max_angular / min_safe）
# 启动相机节点（astra_pro 或 d435）
# 启动深度跟随节点（传入所有参数）
# 所有参数带默认值，零参数即可启动</code></pre>

<h3>可调参数</h3>

<table>
  <colgroup><col width="140"/><col width="100"/><col width="120"/><col width="220"/></colgroup>
  <thead>
    <tr><th background-color="light-gray">参数</th><th background-color="light-gray">默认值</th><th background-color="light-gray">调参方向</th><th background-color="light-gray">实操建议</th></tr>
  </thead>
  <tbody>
    <tr><td>camera_type</td><td>d435</td><td>astra_pro / d435</td><td>根据实际相机型号选择</td></tr>
    <tr><td>desired_distance</td><td>1.5m</td><td>0.8 ~ 2.0</td><td>先 1.5 测试，再调近或调远</td></tr>
    <tr><td>kp_distance</td><td>0.5</td><td>0.2 ~ 1.0</td><td>调大→激进，调小→平滑</td></tr>
    <tr><td>kp_yaw</td><td>2.0</td><td>1.0 ~ 4.0</td><td>调大→快速转向</td></tr>
    <tr><td>max_linear</td><td>0.5 m/s</td><td>0.1 ~ 0.8</td><td>先低后高，防止冲撞</td></tr>
    <tr><td>max_angular</td><td>1.5 rad/s</td><td>0.5 ~ 2.0</td><td>急转甩飞负载时降低</td></tr>
    <tr><td><b>min_safe</b></td><td><b>0.5m</b></td><td><b>0.3 ~ 1.0</b></td><td><span background-color="light-green"><b>核心安全参数，过近自动停转</b></span></td></tr>
  </tbody>
</table>

<h3>🔬 实操步骤</h3>

<callout emoji="🧪" background-color="light-gray" border-color="gray">
  <p><b>环境要求：</b>需要 D435 相机 + ROS2 环境 + colcon build 通过。底盘可选（无底盘时仅运行跟随逻辑不驱动）。</p>
</callout>

<pre lang="bash" caption="编译 + 启动"><code># 1. 编译
cd ~/Music/spark_humble
source /opt/ros/humble/setup.bash
colcon build --packages-select vision_basics
source install/setup.bash

# 2. 默认参数启动（零配置）
ros2 launch vision_basics depth_follow.launch.py

# 3. 自定义参数（近距离慢速跟随，安全优先）
ros2 launch vision_basics depth_follow.launch.py \
  desired_distance:=0.8 max_linear:=0.2 min_safe:=0.3

# 4. 检查话题
ros2 topic list | grep cmd_vel    # 确认速度指令正在发布
ros2 topic echo /cmd_vel           # 查看实时速度值</code></pre>

<callout emoji="👀" background-color="light-blue" border-color="blue">
  <p><b>期望看到：</b></p>
  <ol>
    <li seq="auto">终端输出相机节点启动成功</li>
    <li seq="auto">跟随节点开始订阅点云话题</li>
    <li seq="auto"><code>ros2 topic echo /cmd_vel</code> 显示实时速度值</li>
    <li seq="auto">有人站在相机前方 1.5m 左右时，机器人开始跟随</li>
    <li seq="auto">人靠近到 0.5m 以内时，机器人原地停转（安全限幅生效）</li>
  </ol>
</callout>

<callout emoji="⚠️" background-color="light-yellow" border-color="yellow">
  <p><b>安全提示：</b></p>
  <ol>
    <li seq="auto">首次测试时确保机器人悬空或有紧急停止按钮</li>
    <li seq="auto">先调低 max_linear（如 0.1）确认控制方向正确</li>
    <li seq="auto">min_safe 不要设太小（&lt;0.3m），留足刹车距离</li>
    <li seq="auto">如果机器人乱动，立即 Ctrl+C 停止</li>
  </ol>
</callout>

<h2>📋 实操检查清单</h2>

<checkbox done="false">终端运行 python3 following_controller.py，确认三步输出正确</checkbox>
<checkbox done="false">确认第1步 P 控制在过近时 v=-0.500（后退）</checkbox>
<checkbox done="false">确认第3步 Object 控制在过近时 v=0.000（停转）</checkbox>
<checkbox done="false">colcon build --packages-select vision_basics 编译通过</checkbox>
<checkbox done="false">ros2 launch vision_basics depth_follow.launch.py 启动成功</checkbox>
<checkbox done="false">ros2 topic echo /cmd_vel 能看到实时速度输出</checkbox>
<checkbox done="false">人靠近 0.5m 以内时机器人自动停转</checkbox>

<h2>📚 参考资料</h2>

<ul>
  <li>📖 参考版文档：X3qRdYfoco4XwcxI6Hkc4pXuneb（03-2 基于深度信息的目标跟随）</li>
  <li>🧪 代码路径：~/Music/spark_humble/src/ros2_vision/vision_basics/vision_basics/</li>
  <li>📅 更新：2026-07-18，Git ca9cef9</li>
</ul>
